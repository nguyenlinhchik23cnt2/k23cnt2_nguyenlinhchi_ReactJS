// mormal function
var fn_add = function(x, y){
    let ans = x + y
    console.log(ans);
}

fn_add(10, 20);

//arrow function

const fn_add_arrow = (x, y) => {
    let ans = x + y;
    console.log(ans);
}